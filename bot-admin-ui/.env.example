# Backend origin for the Vite dev proxy. The browser still calls same-origin
# paths (/bot-admin/api/...) so the session cookie works. Use a LAN IP for
# phone testing; open the UI at http://<LAN-IP>:5301/bot-admin/.
VITE_API_BASE_URL=http://localhost:3000
